
services:
  synflow:
    build: .
    container_name: synflow_app
    ports:
      - "8080:80"
    environment:
      - SNAKEMAKE_CORES=4
    restart: unless-stopped
    networks:
      - synflow_network

networks:
  synflow_network:
    driver: bridge
